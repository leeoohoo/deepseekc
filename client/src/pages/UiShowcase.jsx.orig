import React, { useState, useEffect } from 'react';
import { motion, AnimatePresence } from 'framer-motion';
import { X, ZoomIn, Download, FileText, ChevronRight, Maximize2, Monitor, FolderTree, Sparkles, Settings, Grid, Server, Cpu, MessageSquare, Sliders } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useTranslation } from 'react-i18next';

const UiShowcase = () => {
  const { t } = useTranslation();
  const [selectedImage, setSelectedImage] = useState(null);
  const [hoveredIndex, setHoveredIndex] = useState(null);

  // 主要页面数据 (3个)
  const mainPages = [
    {
      id: 'session',
      title: t('uiShowcase.mainPages.session.title'),
      subtitle: t('uiShowcase.mainPages.session.subtitle'),
      description: t('uiShowcase.mainPages.session.description'),
      svg: '/assets/ui-page-svgs/page-session.svg',
      icon: <Monitor className="w-6 h-6" />,
      features: t('uiShowcase.mainPages.session.features', { returnObjects: true }),
      color: 'from-blue-500 to-cyan-500',
      category: 'main'
    },
    {
      id: 'workspace',
      title: t('uiShowcase.mainPages.workspace.title'),
      subtitle: t('uiShowcase.mainPages.workspace.subtitle'),
      description: t('uiShowcase.mainPages.workspace.description'),
      svg: '/assets/ui-page-svgs/page-workspace.svg',
      icon: <FolderTree className="w-6 h-6" />,
      features: t('uiShowcase.mainPages.workspace.features', { returnObjects: true }),
      color: 'from-green-500 to-emerald-500',
      category: 'main'
    },
    {
      id: 'events',
      title: t('uiShowcase.mainPages.events.title'),
      subtitle: t('uiShowcase.mainPages.events.subtitle'),
      description: t('uiShowcase.mainPages.events.description'),
      svg: '/assets/ui-page-svgs/page-events.svg',
      icon: <Sparkles className="w-6 h-6" />,
      features: t('uiShowcase.mainPages.events.features', { returnObjects: true }),
      color: 'from-purple-500 to-pink-500',
      category: 'main'
    }
  ];

  // 设置页面数据 (5个)
  const settingPages = [
    {
      id: 'configuration',
      title: t('uiShowcase.settingPages.configuration.title'),
      subtitle: t('uiShowcase.settingPages.configuration.subtitle'),
      description: t('uiShowcase.settingPages.configuration.description'),
      svg: '/assets/ui-page-svgs/page-admin-models.svg',
      icon: <Cpu className="w-6 h-6" />,
      features: t('uiShowcase.settingPages.configuration.features', { returnObjects: true }),
      color: 'from-orange-500 to-red-500',
      category: 'settings'
    },
    {
      id: 'appearance',
      title: t('uiShowcase.settingPages.appearance.title'),
      subtitle: t('uiShowcase.settingPages.appearance.subtitle'),
      description: t('uiShowcase.settingPages.appearance.description'),
      svg: '/assets/ui-page-svgs/page-admin-mcp.svg',
      icon: <Server className="w-6 h-6" />,
      features: t('uiShowcase.settingPages.appearance.features', { returnObjects: true }),
      color: 'from-indigo-500 to-blue-500',
      category: 'settings'
    },
    {
      id: 'shortcuts',
      title: t('uiShowcase.settingPages.shortcuts.title'),
      subtitle: t('uiShowcase.settingPages.shortcuts.subtitle'),
      description: t('uiShowcase.settingPages.shortcuts.description'),
      svg: '/assets/ui-page-svgs/page-admin-subagents.svg',
      icon: <Grid className="w-6 h-6" />,
      features: t('uiShowcase.settingPages.shortcuts.features', { returnObjects: true }),
      color: 'from-teal-500 to-green-500',
      category: 'settings'
    },
    {
      id: 'plugins',
      title: t('uiShowcase.settingPages.plugins.title'),
      subtitle: t('uiShowcase.settingPages.plugins.subtitle'),
      description: t('uiShowcase.settingPages.plugins.description'),
      svg: '/assets/ui-page-svgs/page-admin-prompts.svg',
      icon: <MessageSquare className="w-6 h-6" />,
      features: t('uiShowcase.settingPages.plugins.features', { returnObjects: true }),
      color: 'from-yellow-500 to-amber-500',
      category: 'settings'
    },
    {
      id: 'advanced',
      title: t('uiShowcase.settingPages.advanced.title'),
      subtitle: 'Runtime Configuration',
      description: t('uiShowcase.settingPages.advanced.description'),
      svg: '/assets/ui-page-svgs/page-admin-settings.svg',
      icon: <Sliders className="w-6 h-6" />,
      features: t('uiShowcase.settingPages.advanced.features', { returnObjects: true }),
      color: 'from-gray-500 to-slate-500',
      category: 'settings'
    }
  ];

  // 技术特点
  const techFeatures = [
    { icon: '🚀', title: t('uiShowcase.techFeatures.realTime.title'), description: t('uiShowcase.techFeatures.realTime.description') },
    { icon: '🎨', title: t('uiShowcase.techFeatures.responsive.title'), description: t('uiShowcase.techFeatures.responsive.description') },
    { icon: '📱', title: t('uiShowcase.techFeatures.keyboardNav.title'), description: t('uiShowcase.techFeatures.keyboardNav.description') },
    { icon: '⚡', title: t('uiShowcase.techFeatures.darkMode.title'), description: t('uiShowcase.techFeatures.darkMode.description') },
    { icon: '🔧', title: t('uiShowcase.techFeatures.search.title'), description: t('uiShowcase.techFeatures.search.description') },
    { icon: '🔄', title: t('uiShowcase.techFeatures.export.title'), description: t('uiShowcase.techFeatures.export.description') }
  ];

  // 打开大图查看
  const openImageModal = (imageSrc, title) => {
    setSelectedImage({ src: imageSrc, title });
  };

  // 关闭大图查看
  const closeImageModal = () => {
    setSelectedImage(null);
  };

  // ESC键关闭模态框
  useEffect(() => {
    const handleKeyDown = (e) => {
      if (e.key === 'Escape' && selectedImage) {
        closeImageModal();
      }
    };
    document.addEventListener('keydown', handleKeyDown);
    return () => document.removeEventListener('keydown', handleKeyDown);
  }, [selectedImage]);

  // 所有页面合并
  const allPages = [...mainPages, ...settingPages];

  return (
    <div className="min-h-screen bg-gradient-to-b from-dark-950 via-dark-900 to-dark-950">
      {/* Hero Section */}
      <section className="relative overflow-hidden pt-24 pb-20 px-4">
        <div className="absolute inset-0 bg-gradient-to-r from-primary-900/20 via-transparent to-purple-900/20" />
        <div className="container mx-auto relative z-10">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            className="max-w-4xl mx-auto text-center"
          >
            <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary-900/30 border border-primary-800/50 backdrop-blur-sm mb-8">
              <Monitor className="w-5 h-5 text-primary-400" />
              <span className="text-primary-300 font-medium">{t("uiShowcase.hero.badge")}</span>
            </div>
            
            <h1 className="text-5xl md:text-7xl font-bold mb-6">
              <span className="gradient-text">{t("uiShowcase.hero.title1")}</span>
              <span className="block text-white mt-2">{t("uiShowcase.hero.title2")}</span>
            </h1>
            
            <p className="text-xl md:text-2xl text-dark-300 mb-10 max-w-3xl mx-auto">
              {t("uiShowcase.hero.description")}
            </p>

            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <Link
                to="/docs"
                className="btn-primary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <FileText className="w-5 h-5" />
                {t('uiShowcase.buttons.viewDocs')}
                <ChevronRight className="w-5 h-5" />
              </Link>
              <Link
                to="/register"
                className="btn-secondary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <Download className="w-5 h-5" />
                {t('uiShowcase.buttons.downloadNow')}
              </Link>
            </div>
          </motion.div>
        </div>
      </section>

      {/* 主要功能页面展示 */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              <span className="gradient-text">{t('uiShowcase.sectionTitles.mainPages')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('uiShowcase.sectionTitles.mainPagesDescription')}
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {mainPages.map((page, index) => (
              <motion.div
                key={page.id}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                viewport={{ once: true }}
                onMouseEnter={() => setHoveredIndex(index)}
                onMouseLeave={() => setHoveredIndex(null)}
                className="card p-6 hover:border-primary-800/50 transition-all duration-300 group"
              >
                {/* 标题区域 */}
                <div className="flex items-center gap-4 mb-6">
                  <div className={`inline-flex p-3 rounded-xl bg-gradient-to-br ${page.color}`}>
                    <div className="text-white">{page.icon}</div>
                  </div>
                  <div>
                    <h3 className="text-2xl font-bold text-white">{page.title}</h3>
                    <p className="text-primary-400 font-mono">{page.subtitle}</p>
                  </div>
                </div>

                {/* SVG展示区域 */}
                <div 
                  className="relative border border-dark-700 rounded-lg overflow-hidden mb-6 cursor-pointer"
                  onClick={() => openImageModal(page.svg, page.title)}
                >
                  <img 
                    src={page.svg} 
                    alt={`${page.title} 界面`}
                    className="w-full h-auto transition-transform duration-300 group-hover:scale-105"
                  />
                  
                  {/* 悬停遮罩层 */}
                  <div className="absolute inset-0 bg-gradient-to-t from-dark-950/90 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
                    <div className="flex items-center gap-2 px-4 py-2 rounded-full bg-primary-900/80 backdrop-blur-sm">
                      <ZoomIn className="w-5 h-5 text-primary-300" />
                      <span className="text-primary-300 font-medium">{t('uiShowcase.buttons.clickToViewLarge')}</span>
                    </div>
                  </div>

                  {/* 角标 */}
                  <div className="absolute top-4 right-4">
                    <div className="px-3 py-1 rounded-full bg-dark-900/80 backdrop-blur-sm border border-dark-700">
                      <span className="text-primary-300 text-sm font-mono">{t('uiShowcase.labels.mainPage')}</span>
                    </div>
                  </div>
                </div>

                {/* 描述区域 */}
                <p className="text-dark-300 mb-6">{page.description}</p>

                {/* 功能列表 */}
                <div className="space-y-2">
                  {page.features.map((feature, idx) => (
                    <div key={idx} className="flex items-center gap-3">
                      <div className="w-2 h-2 rounded-full bg-primary-500" />
                      <span className="text-dark-200">{feature}</span>
                    </div>
                  ))}
                </div>

                {/* 查看大图按钮 */}
                <button
                  onClick={() => openImageModal(page.svg, page.title)}
                  className="mt-6 w-full border border-primary-500 text-primary-400 hover:bg-primary-900/30 transition-colors rounded-lg inline-flex items-center justify-center gap-2 py-3"
                >
                  <Maximize2 className="w-5 h-5" />
                  {t('uiShowcase.buttons.viewLargeImage')}
                </button>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* 设置与管理页面展示 */}
      <section className="py-20 px-4 bg-dark-900/30">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              <span className="gradient-text">{t('uiShowcase.sectionTitles.settingInterfaces')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('uiShowcase.sectionTitles.settingInterfacesDescription')}
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {settingPages.map((page, index) => (
              <motion.div
                key={page.id}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                viewport={{ once: true }}
                className="card p-6 hover:border-primary-800/50 transition-all duration-300 group"
              >
                {/* 标题区域 */}
                <div className="flex items-center gap-4 mb-6">
                  <div className={`inline-flex p-3 rounded-xl bg-gradient-to-br ${page.color}`}>
                    <div className="text-white">{page.icon}</div>
                  </div>
                  <div>
                    <h3 className="text-2xl font-bold text-white">{page.title}</h3>
                    <p className="text-primary-400 font-mono">{page.subtitle}</p>
                  </div>
                </div>

                {/* SVG展示区域 */}
                <div 
                  className="relative border border-dark-700 rounded-lg overflow-hidden mb-6 cursor-pointer"
                  onClick={() => openImageModal(page.svg, page.title)}
                >
                  <img 
                    src={page.svg} 
                    alt={`${page.title} 界面`}
                    className="w-full h-auto transition-transform duration-300 group-hover:scale-105"
                  />
                  
                  {/* 悬停遮罩层 */}
                  <div className="absolute inset-0 bg-gradient-to-t from-dark-950/90 via-transparent to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300 flex items-center justify-center">
                    <div className="flex items-center gap-2 px-4 py-2 rounded-full bg-primary-900/80 backdrop-blur-sm">
                      <ZoomIn className="w-5 h-5 text-primary-300" />
                      <span className="text-primary-300 font-medium">{t('uiShowcase.buttons.clickToViewLarge')}</span>
                    </div>
                  </div>

                  {/* 角标 */}
                  <div className="absolute top-4 right-4">
                    <div className="px-3 py-1 rounded-full bg-dark-900/80 backdrop-blur-sm border border-dark-700">
                      <span className="text-primary-300 text-sm font-mono">{t('uiShowcase.labels.settingsPage')}</span>
                    </div>
                  </div>
                </div>

                {/* 描述区域 */}
                <p className="text-dark-300 mb-6">{page.description}</p>

                {/* 功能列表 */}
                <div className="space-y-2">
                  {page.features.map((feature, idx) => (
                    <div key={idx} className="flex items-center gap-3">
                      <div className="w-2 h-2 rounded-full bg-primary-500" />
                      <span className="text-dark-200">{feature}</span>
                    </div>
                  ))}
                </div>

                {/* 查看大图按钮 */}
                <button
                  onClick={() => openImageModal(page.svg, page.title)}
                  className="mt-6 w-full border border-primary-500 text-primary-400 hover:bg-primary-900/30 transition-colors rounded-lg inline-flex items-center justify-center gap-2 py-3"
                >
                  <Maximize2 className="w-5 h-5" />
                  {t('uiShowcase.buttons.viewLargeImage')}
                </button>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* 技术特点总结 */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              <span className="gradient-text">{t('uiShowcase.sectionTitles.technicalFeatures')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('uiShowcase.sectionTitles.technicalFeaturesDescription')}
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            {techFeatures.map((feature, index) => (
              <motion.div
                key={feature.title}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                viewport={{ once: true }}
                className="card p-8 text-center"
              >
                <div className="text-4xl mb-4">{feature.icon}</div>
                <h3 className="text-2xl font-bold text-white mb-4">{feature.title}</h3>
                <p className="text-dark-300">{feature.description}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* CTA Section */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="card max-w-4xl mx-auto p-12 text-center"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              {t('uiShowcase.cta.heading')}
            </h2>
            <p className="text-xl text-dark-300 mb-10 max-w-2xl mx-auto">
               {t('uiShowcase.cta.subheading')}
            </p>
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <Link
                to="/register"
                className="btn-primary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <Download className="w-5 h-5" />
                {t('uiShowcase.labels.downloadDeepseekCli')}
                <ChevronRight className="w-5 h-5" />
              </Link>
              <Link
                to="/docs"
                className="btn-secondary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <FileText className="w-5 h-5" />
                {t('uiShowcase.labels.viewDetailedDocs')}
              </Link>
            </div>
          </motion.div>
        </div>
      </section>

      {/* 大图查看模态框 */}
      <AnimatePresence>
        {selectedImage && (
          <motion.div
            initial={{ opacity: 0 }}
            animate={{ opacity: 1 }}
            exit={{ opacity: 0 }}
            className="fixed inset-0 z-50 flex items-center justify-center p-4 bg-dark-950/95 backdrop-blur-md"
            onClick={closeImageModal}
          >
            <motion.div
              initial={{ scale: 0.9, opacity: 0 }}
              animate={{ scale: 1, opacity: 1 }}
              exit={{ scale: 0.9, opacity: 0 }}
              className="relative max-w-6xl w-full max-h-[90vh]"
              onClick={(e) => e.stopPropagation()}
            >
              <div className="flex items-center justify-between mb-4">
                <h3 className="text-2xl font-bold text-white">{selectedImage.title} - {t('uiShowcase.labels.interfaceLargeImage')}</h3>
                <button
                  onClick={closeImageModal}
                  className="p-2 rounded-full bg-dark-800 hover:bg-dark-700 transition-colors"
                >
                  <X className="w-6 h-6 text-dark-300" />
                </button>
              </div>
              
              <div className="border border-dark-700 rounded-lg overflow-hidden">
                <img 
                  src={selectedImage.src} 
                  alt={`${selectedImage.title} 大图`}
                  className="w-full h-auto max-h-[70vh] object-contain"
                />
              </div>
              
              <div className="mt-4 flex items-center justify-between text-dark-400">
                <div className="text-sm">
                  使用 <span className="text-primary-400">ESC</span> 键或点击背景关闭
                </div>
                <div className="text-sm">
                  图片尺寸: <span className="text-primary-400">1600×1000px</span>
                </div>
              </div>
            </motion.div>
          </motion.div>
        )}
      </AnimatePresence>
    </div>
  );
};

export default UiShowcase;