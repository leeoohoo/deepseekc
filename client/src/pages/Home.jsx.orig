import { motion } from 'framer-motion';
import {
  Terminal,
  Monitor,
  Store,
  ListChecks,
  Clock,
  Sparkles,
  Download,
  FileText,
  Code,
  Cpu,
  Database,
  Cloud,
  Shield,
  Zap,
  ChevronRight,
  CheckCircle,
  Package,
  Play,
  Command,
  Layers,
  Plug
} from 'lucide-react';
import { Link } from 'react-router-dom';
import { useTranslation } from 'react-i18next';

const Home = () => {
  const { t } = useTranslation();

  // Features data
  const featureKeys = ['subAgentMarketplace', 'taskManagement', 'longRunningCommands', 'autoSummary'];
  const features = featureKeys.map(key => ({
    icon: key === 'subAgentMarketplace' ? <Store className="w-10 h-10" /> :
          key === 'taskManagement' ? <ListChecks className="w-10 h-10" /> :
          key === 'longRunningCommands' ? <Clock className="w-10 h-10" /> :
          <Monitor className="w-10 h-10" />,
    title: t(`home.features.items.${key}.title`),
    description: t(`home.features.items.${key}.description`),
    color: key === 'subAgentMarketplace' ? 'from-purple-500 to-pink-500' :
           key === 'taskManagement' ? 'from-blue-500 to-cyan-500' :
           key === 'longRunningCommands' ? 'from-green-500 to-emerald-500' :
           'from-orange-500 to-red-500',
  }));

  // Common commands table data
  const commandKeys = ['subMarketplace', 'subInstall', 'subRun', 'prompt'];
  const commands = commandKeys.map(key => ({
    command: t(`home.commands.items.${key}.command`),
    description: t(`home.commands.items.${key}.description`),
    icon: key === 'subMarketplace' ? <Store className="w-5 h-5" /> :
          key === 'subInstall' ? <Download className="w-5 h-5" /> :
          key === 'subRun' ? <Play className="w-5 h-5" /> :
          <FileText className="w-5 h-5" />,
  }));

  // Plugin categories
  const pluginCategoryKeys = ['pythonDevelopment', 'frontendReact', 'springBoot', 'cloudInfrastructure', 'cicd', 'securityScanning', 'machineLearning', 'databaseDesign'];
  const pluginCategories = pluginCategoryKeys.map(key => ({
    name: t(`home.plugins.categories.${key}`),
    icon: key === 'pythonDevelopment' ? <Code className="w-6 h-6" /> :
          key === 'frontendReact' ? <Layers className="w-6 h-6" /> :
          key === 'springBoot' ? <Cpu className="w-6 h-6" /> :
          key === 'cloudInfrastructure' ? <Cloud className="w-6 h-6" /> :
          key === 'cicd' ? <Zap className="w-6 h-6" /> :
          key === 'securityScanning' ? <Shield className="w-6 h-6" /> :
          key === 'machineLearning' ? <Database className="w-6 h-6" /> :
          <Package className="w-6 h-6" />,
    count: key === 'pythonDevelopment' ? 12 :
           key === 'frontendReact' ? 8 :
           key === 'springBoot' ? 6 :
           key === 'cloudInfrastructure' ? 10 :
           key === 'cicd' ? 7 :
           key === 'securityScanning' ? 5 :
           key === 'machineLearning' ? 9 :
           4,
  }));

  // Terminal animation commands (keep as is)
  const terminalCommands = [
    '$ deepseekc chat',
    '$ /sub marketplace',
    '$ /sub install python-dev',
    '$ /sub run python-dev "refactor this code"',
    '$ /tool list',
  ];

  return (
    <div className="min-h-screen bg-gradient-to-b from-dark-950 via-dark-900 to-dark-950">
      {/* Hero Section */}
      <section className="relative overflow-hidden pt-24 pb-32 px-4">
        <div className="absolute inset-0 bg-gradient-to-r from-primary-900/20 via-transparent to-purple-900/20" />
        <div className="container mx-auto relative z-10">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            className="max-w-4xl mx-auto text-center"
          >
            <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-primary-900/30 border border-primary-800/50 backdrop-blur-sm mb-8">
              <Terminal className="w-5 h-5 text-primary-400" />
              <span className="text-primary-300 font-medium">{t('home.hero.badge')}</span>
            </div>
            
            <h1 className="text-5xl md:text-7xl font-bold mb-6">
              <span className="gradient-text">{t('home.hero.title1')}</span>
              <span className="block text-white mt-2">{t('home.hero.title2')}</span>
            </h1>
            
            <p className="text-xl md:text-2xl text-dark-300 mb-10 max-w-3xl mx-auto">
              {t('home.hero.subtitle')}
            </p>

            {/* Install command */}
            <div className="mb-12">
              <div className="code-block max-w-2xl mx-auto">
                <div className="flex items-center justify-between mb-2">
                  <span className="text-dark-500 text-sm">{t('home.hero.installLabel')}</span>
                  <button className="text-primary-400 hover:text-primary-300 text-sm">
                    {t('home.hero.copyButton')}
                  </button>
                </div>
                <code className="text-green-400 font-mono text-lg">
                  {t('home.hero.installCommand')}
                </code>
              </div>
            </div>

            {/* CTA Buttons */}
            <div className="flex flex-col sm:flex-row gap-4 justify-center mb-20">
              <Link
                to="/register"
                className="btn-primary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <Download className="w-5 h-5" />
                {t('home.hero.cta.getStarted')}
                <ChevronRight className="w-5 h-5" />
              </Link>
              <Link
                to="/docs"
                className="btn-secondary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <FileText className="w-5 h-5" />
                {t('home.hero.cta.viewDocs')}
              </Link>
            </div>

            {/* UI Preview & Terminal */}
            <motion.div
              initial={{ opacity: 0, scale: 0.95 }}
              animate={{ opacity: 1, scale: 1 }}
              transition={{ duration: 0.6, delay: 0.2 }}
              className="grid grid-cols-1 lg:grid-cols-2 gap-8 max-w-4xl mx-auto"
            >
              {/* Terminal Mockup - Reduced Visual Weight */}
              <div className="bg-dark-900/30 border border-dark-800 rounded-2xl backdrop-blur-xl p-4">
                <div className="flex items-center gap-2 mb-3">
                  <div className="w-2 h-2 rounded-full bg-red-500" />
                  <div className="w-2 h-2 rounded-full bg-yellow-500" />
                  <div className="w-2 h-2 rounded-full bg-green-500" />
                  <span className="text-dark-400 text-sm ml-2">{t('home.hero.terminalLabel')}</span>
                </div>
                <div className="space-y-1">
                  {terminalCommands.slice(0, 3).map((cmd, idx) => (
                    <div key={idx} className="font-mono text-dark-300 text-sm">
                      <span className="text-green-400">$</span>{' '}
                      <span className="text-white">{cmd}</span>
                    </div>
                  ))}
                </div>
              </div>

              {/* Dashboard UI Preview */}
              <div className="bg-dark-900/30 border border-dark-800 rounded-2xl backdrop-blur-xl p-6">
                <div className="flex items-center gap-3 mb-4">
                  <div className="p-2 rounded-lg bg-primary-900/30">
                    <Monitor className="w-6 h-6 text-primary-400" />
                  </div>
                  <div>
                  <h3 className="text-xl font-bold text-white">{t('home.uiPreview.modernInterface')}</h3>
                  <p className="text-dark-300 text-sm">{t('home.uiPreview.visualExperience')}</p>
                  </div>
                </div>
                <div className="border border-dark-700 rounded-lg overflow-hidden mb-4">
                  <img 
                    src="/assets/ui-page-svgs/page-session.svg" 
                    alt="DeepSeek CLI 管理台界面"
                    className="w-full h-auto"
                  />
                </div>
                <div className="flex items-center justify-between">
                  <p className="text-dark-300 text-sm">
                  {t('home.uiPreview.description')}
                  </p>
                  <Link
                    to="/ui-showcase"
                    className="btn-primary inline-flex items-center gap-2 px-4 py-2 text-sm"
                  >
                    <Monitor className="w-4 h-4" />
                  {t('home.uiPreview.viewAllInterfaces')}
                  </Link>
                </div>
              </div>
            </motion.div>
          </motion.div>
        </div>
      </section>

      {/* Features Section */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              <span className="gradient-text">{t('home.features.heading')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('home.features.subheading')}
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-8">
            {features.map((feature, index) => (
              <motion.div
                key={feature.title}
                initial={{ opacity: 0, y: 30 }}
                whileInView={{ opacity: 1, y: 0 }}
                transition={{ duration: 0.5, delay: index * 0.1 }}
                viewport={{ once: true }}
                className="card p-8 hover:border-primary-800/50 transition-all duration-300 group"
              >
                <div className={`inline-flex p-3 rounded-xl bg-gradient-to-br ${feature.color} mb-6`}>
                  <div className="text-white">{feature.icon}</div>
                </div>
                <h3 className="text-2xl font-bold text-white mb-4">{feature.title}</h3>
                <p className="text-dark-300">{feature.description}</p>
              </motion.div>
            ))}
          </div>
        </div>
      </section>

      {/* Quick Start Section */}
      <section className="py-20 px-4 bg-dark-900/30">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              <span className="gradient-text">{t('home.quickStart.heading')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('home.quickStart.subheading')}
            </p>
          </motion.div>

          <div className="grid grid-cols-1 lg:grid-cols-3 gap-8 max-w-6xl mx-auto">
            <motion.div
              initial={{ opacity: 0, x: -30 }}
              whileInView={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.5 }}
              viewport={{ once: true }}
              className="card p-8"
            >
              <div className="flex items-center gap-3 mb-6">
                <div className="p-3 rounded-lg bg-primary-900/30">
                  <Download className="w-8 h-8 text-primary-400" />
                </div>
                <h3 className="text-2xl font-bold text-white">{t('home.quickStart.steps.install.title')}</h3>
              </div>
              <div className="code-block">
                <code className="text-green-400">{t('home.quickStart.steps.install.command')}</code>
              </div>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: 0.1 }}
              viewport={{ once: true }}
              className="card p-8"
            >
              <div className="flex items-center gap-3 mb-6">
                <div className="p-3 rounded-lg bg-purple-900/30">
                  <Terminal className="w-8 h-8 text-purple-400" />
                </div>
                <h3 className="text-2xl font-bold text-white">{t('home.quickStart.steps.startCli.title')}</h3>
              </div>
              <div className="space-y-3">
                <div className="code-block">
                  <code className="text-green-400">{t('home.quickStart.steps.startCli.command')}</code>
                </div>
                <p className="text-dark-400 text-sm">{t('home.quickStart.steps.startCli.note')}</p>
                <div className="code-block">
                  <code className="text-green-400">{t('home.quickStart.steps.startCli.npxCommand')}</code>
                </div>
              </div>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, x: 30 }}
              whileInView={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.5, delay: 0.2 }}
              viewport={{ once: true }}
              className="card p-8"
            >
              <div className="flex items-center gap-3 mb-6">
                <div className="p-3 rounded-lg bg-emerald-900/30">
                  <Command className="w-8 h-8 text-emerald-400" />
                </div>
                <h3 className="text-2xl font-bold text-white">{t('home.quickStart.steps.explore.title')}</h3>
              </div>
              <ul className="space-y-3">
                <li className="flex items-center gap-3 text-dark-300">
                  <CheckCircle className="w-5 h-5 text-emerald-500" />
                  <span>{t('home.quickStart.steps.explore.items.browseMarketplace')}</span>
                </li>
                <li className="flex items-center gap-3 text-dark-300">
                  <CheckCircle className="w-5 h-5 text-emerald-500" />
                  <span>{t('home.quickStart.steps.explore.items.installFirstPlugin')}</span>
                </li>
                <li className="flex items-center gap-3 text-dark-300">
                  <CheckCircle className="w-5 h-5 text-emerald-500" />
                  <span>{t('home.quickStart.steps.explore.items.startAutomating')}</span>
                </li>
              </ul>
            </motion.div>
          </div>
        </div>
      </section>

      {/* UI Showcase Section */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              {t('home.uiShowcase.headingCore')} <span className="gradient-text">{t('home.uiShowcase.headingGradient')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('home.uiShowcase.subheading')}
            </p>
          </motion.div>

          <div className="grid grid-cols-1 md:grid-cols-2 gap-8">
            {/* Dashboard */}
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: 0.1 }}
              viewport={{ once: true }}
              className="card p-6 text-center hover:border-primary-800/50 transition-all duration-300 group"
            >
              <div className="border border-dark-700 rounded-lg overflow-hidden mb-4">
                <img 
                  src="/assets/ui-page-svgs/page-session.svg" 
                  alt="主页界面"
                  className="w-full h-auto transition-transform duration-300 group-hover:scale-105"
                />
              </div>
              <h3 className="text-xl font-bold text-white mb-3">{t('home.uiShowcase.items.dashboard.title')}</h3>
              <p className="text-dark-300 mb-6">{t('home.uiShowcase.items.dashboard.description')}</p>
              <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-dark-800 text-dark-300 text-sm">
                <span>Dashboard</span>
              </div>
            </motion.div>

            {/* File Browser */}
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: 0.2 }}
              viewport={{ once: true }}
              className="card p-6 text-center hover:border-primary-800/50 transition-all duration-300 group"
            >
              <div className="border border-dark-700 rounded-lg overflow-hidden mb-4">
                <img 
                  src="/assets/ui-page-svgs/page-workspace.svg" 
                  alt="文件浏览器界面"
                  className="w-full h-auto transition-transform duration-300 group-hover:scale-105"
                />
              </div>
              <h3 className="text-xl font-bold text-white mb-3">{t('home.uiShowcase.items.fileBrowser.title')}</h3>
              <p className="text-dark-300 mb-6">{t('home.uiShowcase.items.fileBrowser.description')}</p>
              <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-dark-800 text-dark-300 text-sm">
                <span>File Browser</span>
              </div>
            </motion.div>

            {/* Floating Island */}
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: 0.3 }}
              viewport={{ once: true }}
              className="card p-6 text-center hover:border-primary-800/50 transition-all duration-300 group"
            >
              <div className="border border-dark-700 rounded-lg overflow-hidden mb-4">
                <img 
                  src="/assets/ui-page-svgs/page-admin-mcp.svg" 
                  alt="Floating Island 界面"
                  className="w-full h-auto transition-transform duration-300 group-hover:scale-105"
                />
              </div>
              <h3 className="text-xl font-bold text-white mb-3">{t('home.uiShowcase.items.floatingIsland.title')}</h3>
              <p className="text-dark-300 mb-6">{t('home.uiShowcase.items.floatingIsland.description')}</p>
              <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-dark-800 text-dark-300 text-sm">
                <span>Floating Island</span>
              </div>
            </motion.div>

            {/* Configuration */}
            <motion.div
              initial={{ opacity: 0, y: 30 }}
              whileInView={{ opacity: 1, y: 0 }}
              transition={{ duration: 0.5, delay: 0.4 }}
              viewport={{ once: true }}
              className="card p-6 text-center hover:border-primary-800/50 transition-all duration-300 group"
            >
              <div className="border border-dark-700 rounded-lg overflow-hidden mb-4">
                <img 
                  src="/assets/ui-page-svgs/page-admin-settings.svg" 
                  alt="Configuration 界面"
                  className="w-full h-auto transition-transform duration-300 group-hover:scale-105"
                />
              </div>
              <h3 className="text-xl font-bold text-white mb-3">{t('home.uiShowcase.items.configuration.title')}</h3>
              <p className="text-dark-300 mb-6">{t('home.uiShowcase.items.configuration.description')}</p>
              <div className="inline-flex items-center gap-2 px-4 py-2 rounded-full bg-dark-800 text-dark-300 text-sm">
                <span>Configuration</span>
              </div>
            </motion.div>
          </div>

          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.2 }}
            viewport={{ once: true }}
            className="text-center mt-16"
          >
            <Link
              to="/ui-showcase"
              className="btn-primary inline-flex items-center gap-2 px-8 py-4 text-lg"
            >
              <Monitor className="w-5 h-5" />
              {t('home.uiShowcase.viewAllDetails')}
              <ChevronRight className="w-5 h-5" />
            </Link>
          </motion.div>
        </div>
      </section>

      {/* Common Commands Section */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              <span className="gradient-text">{t('home.commands.heading')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('home.commands.subheading')}
            </p>
          </motion.div>

          <div className="card max-w-5xl mx-auto overflow-hidden">
            <div className="overflow-x-auto">
              <table className="w-full">
                <thead>
                  <tr className="bg-dark-800/50">
                    <th className="py-4 px-6 text-left text-dark-300 font-semibold">Command</th>
                    <th className="py-4 px-6 text-left text-dark-300 font-semibold">Description</th>
                    <th className="py-4 px-6 text-left text-dark-300 font-semibold">Icon</th>
                  </tr>
                </thead>
                <tbody>
                  {commands.map((cmd, index) => (
                    <motion.tr
                      key={cmd.command}
                      initial={{ opacity: 0, x: -20 }}
                      whileInView={{ opacity: 1, x: 0 }}
                      transition={{ duration: 0.3, delay: index * 0.05 }}
                      viewport={{ once: true }}
                      className="border-b border-dark-800/50 last:border-0 hover:bg-dark-800/30 transition-colors"
                    >
                      <td className="py-4 px-6">
                        <code className="font-mono text-primary-300 text-lg">{cmd.command}</code>
                      </td>
                      <td className="py-4 px-6 text-dark-300">{cmd.description}</td>
                      <td className="py-4 px-6">
                        <div className="text-primary-400">{cmd.icon}</div>
                      </td>
                    </motion.tr>
                  ))}
                </tbody>
              </table>
            </div>
            <div className="text-center py-6 border-t border-dark-800/50">
              <p className="text-dark-300">
                更多命令请查看文档
              </p>
            </div>
          </div>
        </div>
      </section>

      {/* Architecture Overview */}
      <section className="py-20 px-4 bg-dark-900/30">
        <div className="container mx-auto">
          <div className="grid grid-cols-1 lg:grid-cols-2 gap-12 items-center">
            <motion.div
              initial={{ opacity: 0, x: -30 }}
              whileInView={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.6 }}
              viewport={{ once: true }}
            >
              <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
                <span className="gradient-text">{t('home.architecture.heading')}</span>
              </h2>
              <p className="text-xl text-dark-300 mb-8">
                {t('home.architecture.description')}
              </p>
              
              <div className="space-y-6">
                {['mainAgent', 'subAgents', 'configuration'].map((key, idx) => (
                  <div key={key} className="flex items-start gap-4">
                    <div className={`p-3 rounded-lg ${idx === 0 ? 'bg-primary-900/30' : idx === 1 ? 'bg-purple-900/30' : 'bg-emerald-900/30'}`}>
                      {idx === 0 ? <Command className="w-6 h-6 text-primary-400" /> :
                       idx === 1 ? <Plug className="w-6 h-6 text-purple-400" /> :
                       <FileText className="w-6 h-6 text-emerald-400" />}
                    </div>
                    <div>
                      <h4 className="text-xl font-bold text-white mb-2">{t(`home.architecture.items.${key}.title`)}</h4>
                      <p className="text-dark-300" dangerouslySetInnerHTML={{ __html: t(`home.architecture.items.${key}.description`) }} />
                    </div>
                  </div>
                ))}
              </div>
            </motion.div>

            <motion.div
              initial={{ opacity: 0, x: 30 }}
              whileInView={{ opacity: 1, x: 0 }}
              transition={{ duration: 0.6 }}
              viewport={{ once: true }}
              className="card p-8"
            >
              <div className="bg-dark-950 rounded-lg p-6 mb-6">
                <div className="flex items-center gap-2 mb-4">
                  <div className="w-2 h-2 rounded-full bg-red-500" />
                  <div className="w-2 h-2 rounded-full bg-yellow-500" />
                  <div className="w-2 h-2 rounded-full bg-green-500" />
                </div>
                <div className="space-y-3">
                  <div className="font-mono text-sm">
                    <span className="text-green-400">$</span>
                    <span className="text-white ml-2">deepseekc chat</span>
                  </div>
                  <div className="font-mono text-sm text-dark-400">
                    # {t('home.architecture.experience')}
                  </div>
                  <div className="font-mono text-sm">
                    <span className="text-blue-400">&gt;</span>
                    <span className="text-white ml-2">invoke_sub_agent("python-dev", "refactor")</span>
                  </div>
                  <div className="font-mono text-sm text-dark-400">
                    # {t('home.architecture.experience')}
                  </div>
                  <div className="font-mono text-sm">
                    <span className="text-purple-400">→</span>
                    <span className="text-white ml-2">Running Python refactoring...</span>
                  </div>
                </div>
              </div>
              <p className="text-dark-300 text-center">
                {t('home.architecture.experience')}
              </p>
            </motion.div>
          </div>
        </div>
      </section>

      {/* Plugins Showcase */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="text-center mb-16"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              <span className="gradient-text">{t('home.plugins.heading')}</span>
            </h2>
            <p className="text-xl text-dark-300 max-w-2xl mx-auto">
              {t('home.plugins.subheading')}
            </p>
          </motion.div>

          <div className="grid grid-cols-2 md:grid-cols-4 gap-6 max-w-6xl mx-auto">
            {pluginCategories.map((plugin, index) => (
              <motion.div
                key={plugin.name}
                initial={{ opacity: 0, scale: 0.9 }}
                whileInView={{ opacity: 1, scale: 1 }}
                transition={{ duration: 0.3, delay: index * 0.05 }}
                viewport={{ once: true }}
                className="card p-6 text-center hover:border-primary-800/50 transition-all duration-300 group"
              >
                <div className="inline-flex p-3 rounded-full bg-dark-800/50 group-hover:bg-primary-900/30 transition-colors mb-4">
                  <div className="text-primary-400 group-hover:text-primary-300">{plugin.icon}</div>
                </div>
                <h4 className="text-lg font-bold text-white mb-2">{plugin.name}</h4>
                <div className="inline-flex items-center gap-1 px-3 py-1 rounded-full bg-dark-800 text-dark-300 text-sm">
                  <span>{plugin.count}</span>
                  <span>{t('home.plugins.pluginsCount')}</span>
                </div>
              </motion.div>
            ))}
          </div>

          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6, delay: 0.2 }}
            viewport={{ once: true }}
            className="text-center mt-16"
          >
            <Link
              to="/docs"
              className="btn-primary inline-flex items-center gap-2 px-8 py-4 text-lg"
            >
              <Store className="w-5 h-5" />
              {t('home.plugins.browseAll')}
              <ChevronRight className="w-5 h-5" />
            </Link>
          </motion.div>
        </div>
      </section>

      {/* Final CTA */}
      <section className="py-20 px-4">
        <div className="container mx-auto">
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            whileInView={{ opacity: 1, y: 0 }}
            transition={{ duration: 0.6 }}
            viewport={{ once: true }}
            className="card max-w-4xl mx-auto p-12 text-center"
          >
            <h2 className="text-4xl md:text-5xl font-bold text-white mb-6">
              {t('home.cta.heading')}
            </h2>
            <p className="text-xl text-dark-300 mb-10 max-w-2xl mx-auto">
              {t('home.cta.subheading')}
            </p>
            <div className="flex flex-col sm:flex-row gap-4 justify-center">
              <Link
                to="/register"
                className="btn-primary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <Download className="w-5 h-5" />
                {t('home.cta.buttons.getStartedFree')}
              </Link>
              <Link
                to="/docs"
                className="btn-secondary inline-flex items-center gap-2 px-8 py-4 text-lg"
              >
                <FileText className="w-5 h-5" />
                {t('home.cta.buttons.readDocumentation')}
              </Link>
            </div>
          </motion.div>
        </div>
      </section>
    </div>
  );
};

export default Home;