# ============================================
# DeepSeek CLI Website - 环境变量模板文件
# ============================================
#
# 重要: 此文件是模板，不包含真实敏感信息
# 复制此文件为 .env 并填入实际值
# 不要提交 .env 文件到版本控制系统
#
# ============================================
# 数据库配置 (必需)
# ============================================

# MongoDB 连接字符串
# 格式: mongodb://用户名:密码@主机:端口/数据库名?authSource=认证数据库
# 请替换为实际的 MongoDB 连接信息
MONGODB_URI=your_mongodb_uri_here

# ============================================
# JWT 认证配置 (必需)
# ============================================

# JWT 密钥 (至少32字符)
# 使用命令生成: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=your_jwt_secret_here_至少32字符

# JWT 令牌过期时间
# 格式: 数字 + 单位 (如: 60, "2 days", "10h", "7d")
# 可用单位: ms, s, m, h, d, w, y
JWT_EXPIRES_IN=7d

# ============================================
# 服务器配置 (必需)
# ============================================

# 服务器端口
PORT=3001

# 运行环境: development, production, test
NODE_ENV=development

# 跨域资源共享 (CORS) 允许的源
# 开发环境可以使用 * 允许所有源
# 生产环境应设置为具体的域名，如: https://yourdomain.com
CORS_ORIGIN=*

# ============================================
# 电子邮件配置 (可选 - 如果没有配置，邮件服务将无法工作)
# ============================================

# SMTP 服务器主机
# 示例: smtp.gmail.com, smtp.qq.com, smtp.exmail.qq.com
EMAIL_HOST=your_smtp_host_here

# SMTP 端口 (通常 587 用于 TLS, 465 用于 SSL)
EMAIL_PORT=587

# SMTP 认证用户名 (通常是邮箱地址)
EMAIL_USER=your_email@example.com

# SMTP 认证密码 (可能是邮箱密码或应用专用密码)
EMAIL_PASS=your_email_password_here

# 发件人邮箱地址 (可选，默认: noreply@deepseek-cli.com)
EMAIL_FROM=noreply@deepseek-cli.com

# ============================================
# 错误处理配置 (可选)
# ============================================

# 是否在错误响应中包含详细信息
# 仅在开发环境中设置为 true，生产环境应设置为 false
ERROR_DETAILS=false

# ============================================
# 其他配置 (可选)
# ============================================

# 日志级别: debug, info, warn, error
LOG_LEVEL=info

# API 请求频率限制 (请求/窗口时间)
RATE_LIMIT_WINDOW_MS=900000  # 15分钟 (默认)
RATE_LIMIT_MAX_REQUESTS=100  # 每个IP最大请求数 (默认)

# ============================================
# 快速开始指南:
# ============================================
#
# 1. 复制此模板文件为 .env
#    cp .env.example .env
#
# 2. 编辑 .env 文件，填入实际值
#    - MONGODB_URI: 你的 MongoDB 连接字符串
#    - JWT_SECRET: 生成安全的随机密钥
#    - 其他变量根据需要配置
#
# 3. 生成 JWT 密钥 (在项目根目录运行):
#    node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
#
# 4. 启动服务器:
#    npm run dev    # 开发模式 (带热重载)
#    npm start      # 生产模式
#
# 5. 验证环境变量:
#    服务器启动时会检查必需的环境变量
#    访问 http://localhost:3001/health 检查健康状态
#
# 注意事项:
# - 生产环境中，不要使用默认的 JWT 密钥
# - 生产环境中，CORS_ORIGIN 应设置为具体域名
# - 电子邮件配置是可选的，但不配置会导致邮件功能不可用
# - 确保 .env 文件在 .gitignore 中，不要提交到版本控制
#
# ============================================